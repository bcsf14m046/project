<%= link_to("<<BACK TO LIST",{:action => 'list'},:class => 'back-link') %>
<div class="subject delete">
    <h2>DELETE Subject</h2>

    <%= form_for(:subject ,:url =>{:action => 'destroy'},  :method => :post,:id => @subject.id) do |f| %>
        
        <p> Are You sure you want to delete this parmanently ? </p>
        <p class="refrence-name"> <%= @subject.name %></p>
        <th>ID</th>
        <td><%= f.text_field( :id,:readonly =>true) %> </td>
        <div class="form-buttons">
        <%= submit_tag("Delete Subject") %>
        </div>
                                                                
        <% end %>
</div>
